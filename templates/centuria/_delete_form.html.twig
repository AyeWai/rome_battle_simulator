<form method="post" action="{{ path('app_centuria_delete', {'id': centurium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ centurium.id) }}">
    <button class="btn">Delete</button>
</form>
