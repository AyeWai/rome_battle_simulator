{% extends 'base.html.twig' %}

{% block title %}Centuria{% endblock %}

{% block body %}
    <h1>Centuria</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ centurium.id }}</td>
            </tr>
            <tr>
                <th>Health</th>
                <td>{{ centurium.health }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_centuria_index') }}">back to list</a>

    <a href="{{ path('app_centuria_edit', {'id': centurium.id}) }}">edit</a>

    {{ include('centuria/_delete_form.html.twig') }}
{% endblock %}
