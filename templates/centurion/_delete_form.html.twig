<form method="post" action="{{ path('app_centurion_delete', {'id': centurion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ centurion.id) }}">
    <button class="btn">Delete</button>
</form>
